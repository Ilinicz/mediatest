- cache @shared_collections do
  .jumbotron.text-center
    %h1 Welcome!
    %p Here you can create and share media-collection. 
    %p Explore existing collections or register to create your own.
    %p C'mon!
    = fa_icon 'arrow-down 3x'
  .row
    - @shared_collections.each do |collection|
      .col-sm-4
        .panel.panel-default
          .panel-heading '#{collection.title}' by #{collection.user.email}
          .panel-body      
            %p= collection.description
            = link_to 'View', shared_collection_path(collection), class: 'btn btn-primary btn-block'
